databases:
  - name: campusos-staging-db
    databaseName: campusos_staging
    user: campusos_staging_user
    plan: free

services:
  - type: web
    name: campusos-staging-api
    runtime: node
    rootDir: backend
    plan: free
    buildCommand: npm install
    preDeployCommand: npm run seed
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: APP_ENV
        value: staging
      - key: DB_CLIENT
        value: postgres
      - key: DATABASE_URL
        fromDatabase:
          name: campusos-staging-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: FRONTEND_URL
        value: https://campusos-staging-frontend.onrender.com
      - key: SUPERADMIN_EMAIL
        value: erbol.abdusatov@alatoo.edu.kg
      - key: SUPERADMIN_NAME
        value: Erbol Abdusatov
  - type: web
    name: campusos-staging-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    pullRequestPreviewsEnabled: false
    envVars:
      - key: VITE_API_BASE_URL
        value: https://campusos-staging-api.onrender.com/api
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
