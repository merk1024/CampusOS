databases:
  - name: campusos-production-db
    databaseName: campusos_production
    user: campusos_production_user
    plan: free

services:
  - type: web
    name: campusos-production-api
    runtime: node
    rootDir: backend
    plan: free
    buildCommand: npm install
    preDeployCommand: npm run seed
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DB_CLIENT
        value: postgres
      - key: DATABASE_URL
        fromDatabase:
          name: campusos-production-db
          property: connectionString
      - key: PGSSL_ALLOW_SELF_SIGNED
        value: true
      - key: JWT_SECRET
        generateValue: true
      - key: FRONTEND_URL
        value: https://campusos-production-frontend.onrender.com
      - key: SUPERADMIN_EMAIL
        value: erbol.abdusatov@alatoo.edu.kg
      - key: SUPERADMIN_NAME
        value: Erbol Abdusatov
  - type: web
    name: campusos-production-frontend
    runtime: static
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    pullRequestPreviewsEnabled: false
    envVars:
      - key: VITE_API_BASE_URL
        value: https://campusos-production-api.onrender.com/api
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
